"use strict";

import {Calculator} from "./calculator.js";

// execute function on calculator.js in here
let compute = new Calculator(1);

compute.penjumlahan(1)
compute.pengurangan(1)
compute.perkalian(1)
compute.pembagian(1)
compute.pangkat(1)
compute.akar()
compute.pi().result();
